# ML-Agents Training Requirements - Python 3.13 Compatible
# Alternative approach using newer ML-Agents versions

# Core ML dependencies
numpy>=1.21.0,<2.0
torch>=2.0.0
tensorboard>=2.10.0
h5py>=3.7.0
grpcio>=1.50.0
protobuf>=4.0.0
pyyaml>=6.0
Pillow>=9.0.0
attrs>=22.0.0
cattrs>=22.0.0
cloudpickle>=2.0.0
matplotlib>=3.5.0

# ML-Agents alternatives (if direct install fails)
# Try these in order:
# 1. mlagents (latest)
# 2. stable-baselines3 (alternative RL library)
# 3. Custom training script
